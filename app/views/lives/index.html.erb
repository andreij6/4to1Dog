<h1>Listing lives</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @lives.each do |life| %>
  <tr>
    <td><%= link_to 'Show', life %></td>
    <td><%= link_to 'Edit', edit_life_path(life) %></td>
    <td><%= link_to 'Destroy', life, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Live', new_life_path %>
